[project]
name = "mcp-bank-agent"
version = "0.1.0"
description = "MCP server with bank product search and loan calculator"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
    "mcp>=1.0.0",
]

[project.scripts]
mcp-bank-agent = "server:main"
