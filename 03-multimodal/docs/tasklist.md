## Прогресс итераций

| № | Итерация                               | Цель                                     | Статус |
|---|----------------------------------------|------------------------------------------|--------|
| 1 | Базовый Telegram‑бот                  | Бот отвечает на простые команды          | ✅ Выполнена |
| 2 | Подключение LLM                       | Ответы строятся с помощью модели         | ✅ Выполнена |
| 3 | Поддержка контекста диалога           | Бот учитывает несколько последних сообщений | ✅ Выполнена |
| 4 | Обработка ошибок и устойчивость       | Бот корректно реагирует на сбои          | ✅ Выполнена |
| 5 | Конфигурация и переменные окружения   | Настройки вынесены из кода               | ✅ Выполнена |
| 6 | Роли ассистента и стиль ответов       | Управление тоном и ролью бота            | ✅ Выполнена |
| 7 | Логирование и простая диагностика     | Видно, что происходит внутри             | ✅ Выполнена |
| 8 | Упаковка и запуск в Docker            | Лёгкий запуск в локальном контейнере     | ✅ Выполнена |
| 9 | Индикатор набора сообщения            | Пользователь видит, что бот печатает ответ | ✅ Выполнена |
| 10 | Модель данных и конфигурация          | Transaction, Ledger, новые env‑переменные   | ✅ Выполнена |
| 11 | Учёт транзакций из текста             | FinanceManager, structured output         | ✅ Выполнена |
| 12 | Отчёт о балансе                       | Команда `/balance` и balance_query        | ✅ Выполнена |
| 13 | Фото чеков через VLM                  | Обработка изображений, MODEL_IMAGE        | ✅ Выполнена |
| 14 | Роль финансового советника            | System prompt, полный контекст диалога    | ✅ Выполнена |
| 15 | Транскрибация голосовых сообщений     | Локальный Whisper на GPU, учёт как текст  | ⏳ В работе |

---

## Итерация 1. Базовый Telegram‑бот

- [x] Бот отвечает на `/start` приветственным сообщением.
- [x] Бот отвечает простым эхо‑сообщением на любое текстовое сообщение.
- [x] Проверка: отправить несколько сообщений с разных устройств и убедиться, что ответы приходят стабильно.

## Итерация 2. Подключение LLM

- [x] Бот вместо эхо‑ответа формирует осмысленный текст с помощью LLM.
- [x] Сообщения пользователя передаются в модель, ответ возвращается в чат без дополнительной логики.
- [x] Проверка: задать боту простые вопросы и убедиться, что ответы связаны с вопросами.

## Итерация 3. Поддержка контекста диалога

- [x] Бот хранит для каждого пользователя несколько последних сообщений диалога.
- [x] Ответы учитывают предыдущие реплики в рамках одной сессии.
- [x] Проверка: вести непрерывный диалог из нескольких сообщений и убедиться, что бот помнит контекст.

## Итерация 4. Обработка ошибок и устойчивость

- [x] При ошибке LLM или Telegram бот не падает, а возвращает понятное сообщение пользователю.
- [x] Ошибки фиксируются в логах или консоли для дальнейшего разбора.
- [x] Проверка: искусственно спровоцировать ошибку (например, временно убрать ключ) и убедиться, что бот ведёт себя предсказуемо.

## Итерация 5. Конфигурация и переменные окружения

- [x] Критичные настройки (токены, ключи, модель) читаются из переменных окружения.
- [x] В коде не остаётся «зашитых» секретов и адресов внешних сервисов.
- [x] Проверка: изменить значения переменных и убедиться, что поведение бота корректно меняется.

## Итерация 6. Роли ассистента и стиль ответов

- [x] Определён базовый образ/роль ассистента (например, «дружелюбный технический помощник»).
- [x] Ответы бота соответствуют выбранному стилю и роли во всём диалоге.
- [x] Проверка: задать вопросы в разных контекстах и убедиться в единообразии стиля.

## Итерация 7. Логирование и простая диагностика

- [x] Включено базовое логирование ключевых событий (старт бота, входящие сообщения, ошибки).
- [x] Логи помогают понять, что происходит в боте без чтения кода.
- [x] Проверка: запустить бота, пообщаться с ним и убедиться, что в логах отражаются основные шаги.

## Итерация 8. Упаковка и запуск в Docker

- [x] Собран минимальный Docker‑образ для локального запуска бота.
- [x] Запуск бота в контейнере не отличается по функциональности от локального запуска.
- [x] Проверка: запустить бота в Docker и пройти базовый сценарий диалога из предыдущих итераций.

## Итерация 9. Индикатор набора сообщения

- [x] После получения сообщения пользователя бот показывает индикатор «печатает…» (chat action «typing») до отправки ответа.
- [x] Индикатор отображается в чате Telegram (иконка/подпись «печатает») на время ожидания ответа LLM.
- [x] Проверка: отправить сообщение боту и убедиться, что в чате видно индикатор набора перед появлением ответа.

## Итерация 10. Модель данных и конфигурация

- [x] `Transaction`, `TransactionType`, `Category` — dataclass и enums.
- [x] `Ledger` — in-memory хранилище per `user_id`, расчёт баланса.
- [x] `Config` переведён на `TELEGRAM_BOT_TOKEN`, `OPENROUTER_API_KEY`, `OPENROUTER_BASE_URL`, `MODEL_TEXT`, `MODEL_IMAGE`.
- [x] Проверка: создать транзакции в `Ledger`, убедиться в корректном балансе.

## Итерация 11. Учёт транзакций из текста

- [x] Pydantic‑схемы в `llm/schemas.py` (`ParsedTextMessage`).
- [x] `LlmClient` — метод structured completion.
- [x] `FinanceManager` заменяет `DialogManager`; текст → LLM → запись в `Ledger` → подтверждение.
- [x] Проверка: «кофе 350» → бот подтверждает запись с категорией и суммой.

## Итерация 12. Отчёт о балансе

- [x] Хендлер команды `/balance` в `BotApp`.
- [x] LLM распознаёт `balance_query` в текстовых сообщениях.
- [x] Ответ: «Ваш баланс: +N ₽».
- [x] Проверка: несколько записей → `/balance` → корректная сумма.

## Итерация 13. Фото чеков через VLM

- [x] `BotApp` принимает фото, скачивает файл.
- [x] `LlmClient` — vision‑запрос к `MODEL_IMAGE` со structured output.
- [x] Транзакция сохраняется, бот подтверждает.
- [x] Проверка: фото чека → корректная сумма и описание.

## Итерация 14. Роль финансового советника

- [x] System prompt — финансовый советник (стиль, правила учёта, список категорий).
- [x] Полный контекст диалога передаётся в LLM.
- [x] Проверка: уточняющий диалог («а вчерашний обед?») учитывает предыдущие реплики.

## Итерация 15. Транскрибация голосовых сообщений

**Деплой:** бот и STT на **разных** машинах; на GPU‑ВМ — HTTP‑сервис Whisper, на хосте бота — только `SPEECH_API_URL` (см. `docs/vision.md` §6.4).

- [ ] Зависимости: `faster-whisper` + `httpx` (или встроенный клиент) в `pyproject.toml`; README — `ffmpeg`/CUDA только для STT‑сервера.
- [ ] `speech/speech_server.py` — HTTP `POST /v1/transcribe` (multipart OGG), `ffmpeg` + Whisper на GPU; bind `SPEECH_HOST`.
- [ ] `speech/speech_client.py` — HTTP‑клиент к `SPEECH_API_URL`; опционально `SPEECH_API_KEY`.
- [ ] `speech/audio_converter.py` — OGG → WAV (используется **сервером**, не ботом).
- [ ] `Config`: на боте — `SPEECH_API_URL`, `SPEECH_API_KEY`; на сервере — `SPEECH_MODEL`, `SPEECH_DEVICE`, `SPEECH_COMPUTE_TYPE`, `SPEECH_LANGUAGE`, `SPEECH_HOST`; `.env.example`.
- [ ] `Makefile`: цель `make run-speech-server` (опционально).
- [ ] `BotApp`: хендлер `F.voice`, скачивание OGG, индикатор «печатает», HTTP STT → `FinanceManager.handle_user_message`.
- [ ] Обработка ошибок STT (сеть, 5xx, пустой текст): безопасные сообщения, логирование, бот не падает.
- [ ] Приветствие `/start`: упоминание голосовых сообщений.
- [ ] GPU‑ВМ: открыть порт STT (напр. `11435`), **порт 22 не закрывать**; systemd unit для `speech_server` (опционально).
- [ ] Проверка: бот локально, STT на `176.99.135.118` — голосовое «кофе 350» → запись как у текста.
- [ ] Проверка: STT недоступен / пустое аудио → понятная ошибка, предложение написать текстом.

